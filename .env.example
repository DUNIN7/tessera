# =============================================================================
# TESSERA — Environment Configuration
#
# Copy this file to .env and update values for your environment.
# NEVER commit .env to version control.
#
# Owner: Marvin Percival — marvinp@dunin7.com
# License: BSL 1.1 — github.com/DUNIN7/tessera
# =============================================================================

# ── Application ─────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3100
LOG_LEVEL=debug

# ── Database ────────────────────────────────────────────────────────────
# PostgreSQL 16. In Docker Compose, the service name 'db' resolves.
DB_PASSWORD=tessera_dev_only

# ── Authentication (§10.4) ──────────────────────────────────────────────
# CHANGE THESE IN PRODUCTION. These are development-only defaults.
JWT_SECRET=tessera_dev_jwt_secret_change_in_production

# ── FORAY Protocol ──────────────────────────────────────────────────────
# Blockchain audit trail integration. FORAY must be running.
FORAY_API_URL=http://host.docker.internal:3000

# ── HSM (Future — Production) ──────────────────────────────────────────
# SoftHSM2 is used automatically in development.
# Uncomment for production PKCS#11 HSM.
# HSM_PKCS11_MODULE=/usr/lib/softhsm/libsofthsm2.so
# HSM_SLOT=0
# HSM_PIN=change_me
